services:
  web:
    image: example/web:1.0.0
    expose:
      - "8000"
    networks:
      - application

  worker:
    image: example/worker:1.0.0
    command: run-worker
    networks:
      - application

networks:
  application:

